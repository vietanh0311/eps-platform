# Copy thành .env và điền giá trị thật. KHÔNG commit .env.
DATABASE_URL="postgresql://USER@localhost:5432/eps_dev"
# Sinh bằng: openssl rand -base64 32
AUTH_SECRET=""

# Module 4 — Đồng bộ ScaleF (npm run scalef:sync). Dùng tài khoản dịch vụ riêng, KHÔNG dùng tài
# khoản cá nhân — xem README mục "Đồng bộ ScaleF".
SCALEF_ADMIN_EMAIL=""
SCALEF_ADMIN_PASSWORD=""
# Bỏ trống dùng default: https://ambassador.koc.com.vn/api/admin
SCALEF_ADMIN_BASE_URL=""
